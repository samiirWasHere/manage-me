import { createSlice } from '@reduxjs/toolkit';

export const fetchDataList = createSlice({
  name: 'randomSlice',
  initialState: {},
  reducers: {}
});

export const { yourAction } = yourSlice.actions;

export default yourSlice.reducer;
